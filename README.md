# Sound

**Build:** Run `build.sh`; it will watch for changes to `index.js`

**Run:** Serve the directory over HTTPS, e.g.:

```bash
node_modules/.bin/http-server -p 3000 -c-1 # node server without caching
spacekit sound 3000 # to serve over HTTPS with SpaceKit
```
